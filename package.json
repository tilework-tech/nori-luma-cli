{
  "name": "nori-luma-cli",
  "version": "0.0.0",
  "description": "CLI tool for the Luma event platform API",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "nori-luma": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build && npm test",
    "release": "node scripts/create-release.mjs"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist",
    "LICENSE",
    "LICENSE-ADDENDUM.txt"
  ],
  "dependencies": {
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
